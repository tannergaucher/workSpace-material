import React from 'react';
import NavBar from './NavBar'

const Favorites = (props) => {
    return(
        <div>
            <NavBar/>
            <p>My Favorites</p>
        </div>
    )
}

export default Favorites;